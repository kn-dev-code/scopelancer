import { stripe } from "../stripe";


/// Products
export const starter = await stripe.products.create({

})

export const studio = await stripe.products.create({

})

export const pro = await stripe.products.create({
    
})